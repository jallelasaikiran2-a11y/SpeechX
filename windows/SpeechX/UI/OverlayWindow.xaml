<Window x:Class="SpeechX.UI.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        Focusable="False"
        ShowActivated="False"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="BarStyle" TargetType="Rectangle">
            <Setter Property="Width" Value="4" />
            <Setter Property="Height" Value="6" />
            <Setter Property="RadiusX" Value="2" />
            <Setter Property="RadiusY" Value="2" />
            <Setter Property="Fill" Value="White" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="OverlayButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="24" Padding="12,12" Background="#E6202020"
            BorderBrush="#40FFFFFF" BorderThickness="1">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <Button Style="{StaticResource OverlayButton}" Content="✕" Width="28" Height="28" FontSize="14" Click="OnCancelClick" Margin="0,0,10,0"/>
            
            <StackPanel x:Name="Bars" Orientation="Horizontal" VerticalAlignment="Center">
                <Rectangle x:Name="Bar1" Style="{StaticResource BarStyle}" Margin="0,0,5,0" />
                <Rectangle x:Name="Bar2" Style="{StaticResource BarStyle}" Margin="0,0,5,0" />
                <Rectangle x:Name="Bar3" Style="{StaticResource BarStyle}" Margin="0,0,5,0" />
                <Rectangle x:Name="Bar4" Style="{StaticResource BarStyle}" />
            </StackPanel>

            <TextBlock x:Name="TranscriptText"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       Foreground="White"
                       FontSize="13"
                       MaxWidth="360"
                       TextTrimming="WordEllipsis"
                       Visibility="Collapsed" />
                       
            <Button Style="{StaticResource OverlayButton}" Content="✓" Width="28" Height="28" FontSize="16" FontWeight="Bold" Click="OnConfirmClick" Margin="10,0,0,0"/>
        </StackPanel>
    </Border>
</Window>
