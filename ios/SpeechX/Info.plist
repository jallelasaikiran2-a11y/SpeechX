<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Merged with Xcode's generated Info.plist (GENERATE_INFOPLIST_FILE=YES).
	     The speechx:// scheme is how the keyboard opens the app to dictate. -->
	<!-- Only standard HTTPS/ATS crypto — skips the export-compliance question
	     on every TestFlight upload. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- Keeps the mic + Deepgram stream alive after we bounce back to the host
	     app — the whole point of the background-dictation flow. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>audio</string>
	</array>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>ai.vocallabs.speechx</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>speechx</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
